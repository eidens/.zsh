### zsh options, adapted from https://scriptingosx.com/2019/06/moving-to-zsh-part-3-shell-options/

# case-insensitive globbing
setopt NO_CASE_GLOB
# command spellchecking
setopt CORRECT
setopt CORRECT_ALL
# automatic cd if command is a directory
setopt AUTO_CD

## shell history
HISTSIZE=10000
SAVEHIST=10000
HISTFILE=${ZDOTDIR:-$HOME}/.zsh_history
# more command infos
setopt EXTENDED_HISTORY
# append to history
setopt APPEND_HISTORY
# adds commands as they are typed, not at shell exit
setopt INC_APPEND_HISTORY
# expire duplicates first
setopt HIST_EXPIRE_DUPS_FIRST
#ignore duplicates when searching
setopt HIST_FIND_NO_DUPS
# removes blank lines from history
setopt HIST_REDUCE_BLANKS
# verify/change substituted commands before execution
setopt HIST_VERIFY


